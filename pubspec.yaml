name: inquiry
description: A package for easy show inquiry (questionnaire, quiz)
version: 0.0.1
repository: https://github.com/Ch4rl3B/inquiry

environment:
  sdk: '>=2.18.0 <3.0.0'
  flutter: ">=3.3.0"

dependencies:
  flutter:
    sdk: flutter
  percent_indicator: ^4.2.2
  stacked: ^3.1.0+3


dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.1

flutter:
